'use strict'; 

const route = require('../route/route-auth.js');

describe('Router Authorization Module', function () {
  describe('#res.status(201)', function () {
    it('should give us a res.status 201', function () {
      expect(user.generatePasswordHash(pw)).tobe(something)
    });
  });
});